import { HigherUpvotesDirective } from './higher-upvotes.directive';

describe('HigherUpvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new HigherUpvotesDirective();
    expect(directive).toBeTruthy();
  });
});
